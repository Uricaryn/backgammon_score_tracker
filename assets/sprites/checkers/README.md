# Checker sprites (optional)

Add orthographic PNGs (sRGB):

- `checker_white.png`
- `checker_black.png`

Recommended height: 128px (@1x). The app falls back to gradient discs when these are missing.
